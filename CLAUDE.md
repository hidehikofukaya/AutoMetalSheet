# AutoMetalSheet - 板金設計自動化システム

## プロジェクト概要
板金設計の自動化システム。数学的・材料力学的に正確な板金CADデータの自動生成を目標とする。

知識ベース: `docs/KB/00_INDEX.md` から読む。

---

# 学習結果を評価するときの習慣

**この節は、実際に起きた誤りから作られている。**各項目に、それが生まれた事故を併記する。
一般論ではなく、この課題で実際に間違った測定の再発防止である。

**最重要の1行**: この課題では**数値が9回、画像が2回、私を騙した。**
どちらか一方で結論を出してはいけない。

---

## A. 数値を1つでも信じる前に

### A1. 距離を測る前に、両側を同じ間隔に再サンプルする

**4回踏んだ。**すべて「疎な側」と「密な側」を比べて、疎であること自体を誤差として
計上していた。

| 事故 | 出た値 | 実際 |
|---|---|---|
| LINE辺(2点)対 密な表面点群 | 「曲率の7.8%が曲げ線から遠い」 | **0.0%** |
| 2点の教師 対 40点の再構成 | 曲げフレーム往復誤差 9.337mm | **0.000mm** |
| 辺ごと等間隔の描画 | 既知の閉正方形が端点54.2% | **2.4%** |
| ビード中心線を稜線として測定 | 曲率AUC 0.570 | **0.764** |

```python
# 必ずこれを通してから cKDTree に入れる
def dens(c, step=1.0):
    s = np.concatenate([[0], np.cumsum(np.linalg.norm(np.diff(c, axis=0), axis=1))])
    w = np.linspace(0, s[-1], max(int(s[-1]/step)+2, 2))
    return np.stack([np.interp(w, s, c[:, d]) for d in range(3)], 1)
```

### A2. 単位を確認する

フレーム座標は**締結点間距離で正規化**されている。**生の 0.039 は 0.039mm ではない。**
100mm の締結間隔なら 3.9mm。

これで**誤ったFAIL判定を発表した**(絶対0.2mmの閾値を正規化値に適用)。
mmに直すと 6.16 対 23.20mm で、判定は逆だった。

### A3. 結合キーが一意か確かめる

サイドカー2300ファイルのうち**固有のstemは250個だけ**で、各IDが9〜11グループに重複。
stemをキーにした辞書が上書きされ、**全部品を別部品の特徴と比較**していた
→ 「90.6%が説明不能」。正しいキー(`source_stp`)で 1.0%。

**joinする前に `len(set(keys)) == len(files)` を確認する。**

### A4. 答えが分かっている図形で測定器を較正する

閉ループが**構造的に保証されている**フレームが端点0.120と出た。
**閉じていることが分かっている正方形**で試すと54.2% → 描画の欠陥だと判明。

> **構造的に保証されているはずの性質が測定で崩れていたら、まず測定器を疑う。**

### A5. 何を数えているか確認する

ARCの**中点**を分岐点として数え、「外形頂点の33%が次数1」「2300/2300部品が破綻」と
報告し、CATIA側への修正依頼を出す寸前だった。**端点だけ数えると次数1は0、99.9%が閉。**

---

## B. 設定や実行を比較するとき

### B1. 複数回の独立した抽出で比べる

**形状の数値には ±0.2mm 程度の抽出ノイズがある。**

- λ=10 を「無料」と報告 → 2回目の抽出で0.85mm悪化。撤回。
- λ=3 を2回の抽出で採用 → 3回で測ると λ=2 が上回った。

**1回の抽出で選んだ設定は、選んだことにならない。**

### B2. 基準は「1回の生成」ではなく「複数回の平均」

中央選択 K=5 を、基準を任意の1回の生成に取って測り「効果は0.5mm」と報告。
K=9・基準を9回の平均にすると **7.77 → 5.47mm(2.3mm)**だった。

### B3. エポックだけでなく実時間でも揃える

文脈が増えると1エポックが2倍遅くなる(4.3秒 → 8.7秒)。
エポックを揃えた比較は、遅い側に有利に働く。**両方で見る。**

### B4. 損失は合格条件ではない

外れ値注入のスケールバグで面の誤差が 9mm → 39.2mm に崩壊したとき、
**val損失は 0.1374 で、元の 0.1418 より「良かった」。**警告が一切出なかった。

**壊れたモデルでも損失は下がる。合格条件は損失と別に置く。**

### B5. プローブは少数部品なので決定的ではない

epoch15の3.11mmを決定的と報告 → ep20-30で5.00/8.30。12部品のプローブノイズ。

---

## C. 数値と画像

### C1. 集合統計は配置の崩れを見ない

曲げストランドが**絵で明らかにおかしい**のに、5つの指標すべてが「教師と同等」と報告:

| 指標 | 教師 | 生成 |
|---|---|---|
| 長さ中央値 | 32.0mm | 40.0mm |
| 最近傍と平行 | 81% | 62% |
| 対の間隔 | 5.5mm | 6.1mm |
| 方向数 | 7 | 8 |
| **線らしさ** | **0.684** | 0.635 ← **教師が最下位** |

**この上に「合格」を出して撤回した。**
**指標を1つ足して再判定する前に、絵を見る。**

### C2. 画像も嘘をつく

**2回、絵から読んだ欠陥が実在しなかった。**

| 目視の主張 | 実測 | 正体 |
|---|---|---|
| 「生成が一貫して教師より小さい」 | 周長比1.015、小さいのは48% | 教師を太いグレーで描き、両側にはみ出していた |
| 「自己交差している」 | **0%**(教師も0%) | 3次元投影で奥行きの違う2箇所が重なった |

**描画の設定そのものが偽の欠陥を作る。**

### C3. 全体図は微細な欠陥を隠す。必ず拡大する

外形トークンが「37.5°/step 折れている」のに全体図では滑らかに見え、
**「見えないレベル」と誤って報告した。**拡大すると 14°(実用可能)〜95°(破綻)の
大きなばらつきが実在した。

**部品ごとに枠を詰める / 一部を拡大する / 全点を打つ。**

### C4. 描画のコツ(偽の欠陥を作らないため)

- 教師と生成は**同じ線幅**で描く(太い下地は偽の「内側寄り」を作る)
- **斜めから**見る(エッジオンは平坦に潰れて判断不能になった)
- 部品ごとに**枠を詰める**(固定枠では部品が小さすぎて読めなかった)

---

## D. その測定が何を意味するか

### D1. オラクルを先に測る

**最も有利な条件で効かなければ、それ以下の条件でも効かない。**

曲率場の条件付けを、**真の表面・真の曲率**で試して30%悪化。
生成された曲率場を作る前に否定できた。

### D2. 誤差を「選べていない分」と「情報が足りない分」に分解する

同じ部品を K 回生成して:

| | 値 | 意味 |
|---|---|---|
| 単発 | 7.77mm | 今得られるもの |
| K回の中央選択 | 5.47mm | 選択で取れる分 |
| K回の最良(オラクル) | 4.12mm | **新しい情報なしでは縮まない下限** |

**単発とオラクルの差が「選べていない分」。**ここが大きければ選択と条件付けが効く。

### D3. 訓練誤差と検証誤差を比べて、効く手を決める

| | 教師との誤差 |
|---|---|
| **訓練**部品 | **9.03mm** |
| **検証**部品 | **7.44mm** |

**訓練誤差のほうが大きい = 過学習ゼロ。**データを足す先がない。
訓練誤差も下がっていないので容量不足でもない。→ **条件を増やすことだけが効く。**

### D4. 相関を導出と読み違えない

`surface_frame` が2本の曲げ線から**等距離(比1.00)**である → 「中線である」と解釈 →
構築器を作って失敗(構築→真が6.55mm、条件を足すほど16.22mmに悪化)。

**等距離は接合点の近傍でも成り立つ。**近接性(99.9%が20mm以内)が示すのは
「**局所にある**」ことだけで、「**導出できる**」ことではない。

### D5. 答えを与えた評価は評価ではない

特徴生成器の3.89mmは、**真の外形を与えた上でその外形上の位置**を出したもの。
難しい部分(部品がどこにどの大きさであるか)が既に与えられている。

**評価は必ず実際の入力(締結点のみ)から通しで行う。**

### D6. 教師データの値を床にする

「曲がっているのに曲げ線から遠い表面 0%」も「ねじれ 0.004mm」も、
**教師データで測った値が下限**。ゼロを目標にしない。

情報限界: **締結条件がほぼ同一の部品どうしが 14.9mm 違う。**
→ 教師との距離を合格条件にできない。**判定は相対値か、物理的妥当性で。**

---

## E. 報告するとき

### E1. 通知やサマリの数字を報告に使わない

**3回やった。**監視通知の数値をログを見ずに報告し、実際とは違っていた
(7.31/9.24 と報告、実際は 7.59/9.63)。**必ずログの実体を見る。**

### E2. 「測定済み」と「目視」を分けて書く

C2の2件は、目視を測定として報告して撤回した。
**測っていないことは「未検証」と明示する。**

### E3. 判定が崩れたら明示的に撤回する

この会話で撤回したもの: 曲げ段のPASS判定、条件C の7.8%、λ=10、
`surface_frame` の導出可能性、「ジグザグは見えないレベル」。

**撤回は簡潔に、1度だけ、次に進む。**

---

## F. 評価の前に決めておくこと

1. **合格条件を先に書く**(損失とは別に)
2. **その条件は相対値か**(絶対mm閾値は置けない)
3. **オラクルで上界を測れるか**
4. **絵で見るなら、どう描けば偽の欠陥が出ないか**

---

# 設計上の原則(KB 03 より)

> **性質は表現に埋め込め。損失に書くな。**

この形で3回勝ち、逆をやって4回負けた。

| 欲しい性質 | 表現に埋め込んだ形 | 結果 |
|---|---|---|
| 閉ループ | 順序付きリング | 端点 53% → 2.2% |
| 線らしさ | パラメトリック辺(点ではなく辺を出す) | 端点 0.000 |
| 平面性 | 辺ごとの法線 + 自己整合 | ねじれ 5.5分の1 |
| **閉曲線** | **最終変位を残差として計算** | **摂動しても厳密に閉じる** |

> **本数を先に決めない。**折り2本・ビード1つ・稜線4本・6スロット — すべて誤りだった。
> **本数は観測結果であって仕様ではない。**

> **既に持っている情報から導出できるものを条件に足しても効かない**(KB 12、4回失敗)。
