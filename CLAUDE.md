# AutoMetalSheet - 板金設計自動化システム

締結点のみを入力に、板金中立面のワイヤーフレームをE2E生成する。
**形状ルールは一切書かない。**すべて学習で獲得する。

経緯・数値・現在地: **`docs/KB/00_INDEX.md`**
本ファイルは**哲学と習慣だけ**を置く。記録は KB に置く。

---

# 開発の哲学

## 1. 性質は表現に埋め込め。損失に書くな

**この形で4回勝ち、逆をやって4回負けた。例外はまだ無い。**

| 欲しい性質 | 表現に埋め込んだ形 |
|---|---|
| 閉ループであること | 順序付きリング(点集合は帯を許す) |
| 線であること | 辺そのものを出す(点は「線に乗る」を学習しなければならない) |
| 平面であること | 辺ごとの法線 + モデル自身の出力への整合 |
| 閉曲線が閉じること | 最終変位を残差として計算(モデルから読まない) |
| 折り線が直線であること | 角トークン(直線は2トークン。点列は点ごとに揺れる) |
| 締結座面を侵さないこと | サンプリング時の拘束(`sample(constrain=)`。入力から導ける硬い拘束は `pin` と同格) |

損失に書いて負けたもの: 密度均一化(2回)、法線の教師付けのみ、曲率制約、関係損失D1/D2。
**代理指標は下がり、直したかったものは直らない。**

例外的に損失側で成功したのは、**目標そのものを性質を満たすように加工**した場合
(`smooth_curve`)。重み調整が不要で、当てはめたモデルが性質を継承する。

## 2. 本数を先に決めない

**折り2本・ビード1つ・稜線4本・6スロット・28曲線 — すべて誤りだった。**
実際は部品ごとに 11〜65 本。

> **本数は観測結果であって仕様ではない。**
> テンソルの大きさは**容量**(メモリ予算)であって個数ではない。

同じ誤りの一般形: **分類を先に決めない。**`fold/bead/flange` の3値one-hotは、
実測すると主成分2つで98%の連続体で、最近傍平均での復元は73%しかなかった。
リブ・ルーバー・ディンプルが入れば区別は消える。

## 3. 導出できる情報を条件に足しても効かない

**4回失敗した**(自己出力、一括点群×2、真の曲率場)。
真の曲率場という最も有利な条件で30%悪化し、計算量は2倍になった。

判定基準はひとつ: **その情報は、既に与えたものから原理的に計算できるか。**
できるなら効かない。無視すべき行が増えてアテンションが薄まるぶん、むしろ悪化する。

効いた条件は1つだけ: **外形 → 曲げ**。外形は締結点から導出できない**設計上の決定**を
含む(潰すと折りの38%が部品外へ)。

## 4. 特定形状への特化を許容しない

どんな構造・どんな締結点の板金でも学習・生成できること。

- 固定枠を作るときは、**それが容量なのか個数なのか**を明示する
- データを数えて枠を決めたなら、それは**特化**である
- 「今のデータには足りている」は、汎用であることの証明ではない

## 5. 意味のあるスロットには位置エンコーディングが要る

**5回踏んだ。**スロットに構造があるのに符号化しないと、モデルは正しい**多重集合**を
作りながら、置く場所を任意にする。

| 構造 | 符号化 |
|---|---|
| 閉ループ | 周期的(`ring_pe`) |
| 開いた線分 | 非周期(`strand_pe`。両端は「2つの端」であって隣ではない) |
| 短い順序付きリスト | 素の正弦(`slot_pe`) |
| 順不同の集合 | なし |

---

# 評価の習慣

**この課題では数値が9回、画像が2回、誤った結論を出させた。**うち5件は発表まで至り撤回した。
**どちらか一方で結論を出してはいけない。**

事故の詳細: `docs/KB/05_measurement_pitfalls.md`

## A. 数値を1つでも信じる前に

| # | 習慣 | 事故 |
|---|---|---|
| A1 | **距離を測る前に、両側を同じ間隔に再サンプルする** | **4回。**最悪のケースでは、存在しない不足を抽出側への依頼書として発表した |
| A2 | **単位を確認する**(フレーム座標は締結点間距離で正規化。生の0.039は0.039mmではない) | 判定が逆になったFAIL報告 |
| A3 | **結合キーが一意か確かめる** | 2300ファイルの固有IDが250個 → 全部品を別部品と比較 |
| A4 | **答えが分かっている図形で測定器を較正する** | 閉正方形が端点54.2%と出て、描画の欠陥が判明 |
| A5 | **何を数えているか確認する** | ARC中点を分岐点と誤認し、CATIA修正依頼の寸前 |

```python
# cKDTree に入れる前に必ず通す
def dens(c, step=1.0):
    s = np.concatenate([[0], np.cumsum(np.linalg.norm(np.diff(c, axis=0), axis=1))])
    w = np.linspace(0, s[-1], max(int(s[-1]/step)+2, 2))
    return np.stack([np.interp(w, s, c[:, d]) for d in range(3)], 1)
```

## B. 設定や実行を比較するとき

| # | 習慣 | 事故 |
|---|---|---|
| B1 | **複数回の独立した抽出で比べる**(形状値には ±0.2mm のノイズ) | 1回の抽出で選んだ設定を2度撤回 |
| B2 | **基準は「1回の生成」ではなく「複数回の平均」** | 効果を0.5mmと過小評価(実際2.3mm) |
| B3 | **エポックだけでなく実時間でも揃える** | 文脈が増えると1エポックが2倍遅い |
| B4 | **損失は合格条件ではない。別に置く** | 誤差が9→39mmに崩壊したのにval損失は改善 |
| B5 | **プローブ(少数部品)は決定的ではない** | ep15の値を決定的と報告し撤回 |

## C. 数値と画像

| # | 習慣 | 事故 |
|---|---|---|
| C1 | **指標を1つ足して再判定する前に、絵を見る** | 5指標すべてが「同等」、うち1つは教師が最下位 |
| C2 | **画像も嘘をつく。目視した欠陥は測ってから主張する** | 「縮んでいる」「交差している」— どちらも実在せず |
| C3 | **全体図は微細な欠陥を隠す。必ず拡大する** | 「見えないレベル」と報告したが、拡大すると破綻していた |

**描画で偽の欠陥を作らないために**:
教師と生成は**同じ線幅**で描く / **斜めから**見る(エッジオンは潰れる) /
部品ごとに**枠を詰める** / 疑うときは**全点を打つ**。

## D. その測定が何を意味するか

| # | 習慣 | 使い方 |
|---|---|---|
| D1 | **オラクルを先に測る** | 最も有利な条件で効かなければ、それ以下でも効かない |
| D2 | **誤差を分解する**(単発 / K回の中央選択 / K回の最良) | 単発と最良の差が「選べていない分」。ここが大きければ選択と条件付けが効く |
| D3 | **訓練誤差と検証誤差を比べる** | 訓練>検証なら過学習ゼロ = データ増は効かない |
| D4 | **相関を導出と読み違えない** | 「等距離」を「中線」と読んで構築器が失敗 |
| D5 | **答えを与えた評価は評価ではない** | 必ず実際の入力(締結点のみ)から通しで測る |
| D6 | **教師データの値を床にする** | ゼロを目標にしない。情報限界14.9mm |
| D7 | **形の一致ではなく「理由を説明できるか」で評価する**(`wtok/rational.py`) | 層1製造可能性 → 層2簡潔性(余剰回転) → 層3機能(座面余裕比)。近傍Chamfer 3.3mmで「良い」と読んだ外形が座面を24%削っていた |
| D8 | **選択で拾えない欠陥は表現で直す** | 9回のオラクルで座面≥1が43%なら再ランクでは届かない。曲げ線の余剰回転12倍は全描画に共通 → トークン表現の問題 |

## E. 報告するとき

- **通知やサマリの数字を報告に使わない。ログの実体を見る**(3回やった)
- **「測定済み」と「目視」を分けて書く。**測っていないことは「未検証」と明示する
- **判定が崩れたら明示的に撤回する。**簡潔に、1度だけ、次に進む

## F. 評価を始める前に決めておく

1. **合格条件を先に書く**(損失とは別に)
2. その条件は**相対値か**(絶対mm閾値は置けない)
3. **オラクルで上界を測れるか**
4. 絵で見るなら、**どう描けば偽の欠陥が出ないか**

---

# 現在地(2026-09-01)

| 段 | 状態 | 実装 |
|---|---|---|
| 締結点フレーム | **任意個数(N≥2)に一般化済み** | `wtok/meshgen.py` |
| 曲げ線の抽出 | **count-free。**曲率の被覆は完全(0.0%) | `wtok/bendlines.py` |
| 曲げ線の表現 | 密トークンはジグザグ(余剰回転 教師比12×)。角トークン(`--corner-t 0.25`)は `end` の取りこぼしで曲線間を飛ぶ → **区切りを構造で保証する表現(変位+角)が次** | `wtok/tokens.py`, KB 17.5 |
| 外形 | **CATIAエッジ分割の教師 + G1連続値チャンネル + 実現時G1強制 + despike**(KB 18.8: g0_missed 0、座面100%、スパイク0%、近傍2.1mm。標準ckpt `runs/frame_g1sm2`。近傍改善は部品数2200と交絡、帰属未分離)。残: モード取り違え4/30部品(選択不能と実証、KB 18.7) | `wtok/frame.py`, `wtok/convert.py`, `wtok/rational_eval.py` |
| 評価 | **3層合理性**(製造可能性/簡潔性/機能)。Chamfer・is_arc は評価から外した | `wtok/rational.py`, KB 17 |
| 面 | 未着手 | — |
| B-rep | 先送り(`surface_frame` の生成が要る) | — |

**破棄済み**(記録として残置): `wtok/feature.py`(種別one-hot)、
`wtok/fillet.py`(構築器、失敗)、点群方式の曲げ各種。

**データ側の制約**: 穴なし、締結点は常に2、折りは常に2。
締結点1個/3個以上、折り3本以上、ビード2個以上は**検証不能**。
