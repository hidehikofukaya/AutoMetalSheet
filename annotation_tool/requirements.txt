# Mirrors the verified versions from _archive_pre_annotation_tool/biw_poc/requirements-r0.txt
# (model-training-only deps such as torch/h5py are intentionally omitted; this tool
# is annotation-only per docs/annotation_tool_design.md SS2).
numpy==2.3.5
scipy==1.16.3
trimesh==4.12.2
pyvista==0.48.4
pyvistaqt==0.11.4
PyQt5==5.15.11
PyYAML==6.0.3
pytest==8.4.2
rtree

# Verified separately, best installed from conda-forge (no universal pip wheel):
# pythonocc-core
