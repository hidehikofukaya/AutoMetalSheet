[project]
name = "annotation_tool"
version = "0.1.0"
description = "Joint annotation tool for BIW sheet-metal assemblies (see docs/annotation_tool_design.md)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
