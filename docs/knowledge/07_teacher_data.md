# 教師データ: 力学×幾何、CATIAエッジ(KB 13, 18)

> 統合日 2026-09-05。以下は元文書を順に**そのまま**収録(見出し番号は元のまま。`KB 21.24` のような参照はこのファイル内を検索)。
> 収録元: `KB\13_mechanics_geometry.md`, `KB\18_catia_edges.md`


---

<!-- 元文書: KB\13_mechanics_geometry.md -->

# 13. 材料力学 × 幾何 — 何を教師として絞り取れるか

Date: 2026-08-31
目的: CATIA データから、曲げ線・外形線・凹凸構造を特定するための情報を最大限引き出す。
制約: **KB 12 の法則** — 既に持っている情報から導出できるものを条件に足しても効かない。
したがって候補は必ず **(a) 抽出可能か (b) 導出可能でないか (c) 実際に情報を持つか** の
3点で分類する。

---

## 13.0 結論を先に

| 使い方 | 何を | 根拠 |
|---|---|---|
| **入力(条件)** | 板厚、曲げ半径の下限、プレス方向、相手部品 | 締結点から導出できない |
| **教師信号(補助損失)** | 可展性、展開図、曲率場 | 導出できるので入力にはならないが、**教師にはなる** |
| **検証** | 可展性、面積保存、曲げ半径の下限、自己交差 | 物理的に必須で、教師の値が床になる |
| **表現の構造** | 可展性を満たす表現(ルーリング) | 「性質は表現に埋め込め」 |

**入力に足せるものは実は少ない。**大半は「導出できる」側にあり、KB 12 が適用される。
ただし**教師信号としては全部使える** — これが今回の主要な発見。

---

## 13.1 板金の中立面は可展面である — 最強の幾何拘束

### 学術的事実

板金は平板を**等長変形**(曲げ・巻き)で作る。等長変形は **Gauss の驚異の定理**により
**Gauss曲率 K を保存する**ので、平板(K=0)から作られる中立面は**至るところ K=0**。

帰結:
- 主曲率の一方が常にゼロ(κ₁·κ₂ = 0)
- ゼロでない主曲率の方向に垂直な**直線(ruling)**が面上を走る
- 面は「無限に密に並んだ直線の族」として構成される
  ([Wolfram MathWorld](https://mathworld.wolfram.com/DevelopableSurface.html))

**曲げ線とは、κ_max が 0 から非ゼロへ遷移する境界**である。分類ではなく、場の性質。

### 現在の抽出との対応

抽出器の `face_geo` が既にこれを分類している:

```
face_geo: ['single_curved', 'double_curved', 'planar', ...]
```

- `planar` — κ₁=κ₂=0
- `single_curved` — 円筒。κ₁≠0, κ₂=0(**可展**)
- `double_curved` — 球・自由曲面。**可展でない**

**注意: 実データには `double_curved` が存在する。**ビードの四隅の球パッチなどで、
厳密には可展でない(伸びを伴う成形)。「板金は全て可展」は近似である。

### 実測(30部品、曲げ線からの距離4mm以内を正例とした AUC)

| 量 | AUC |
|---|---|
| 法線回転率 \|grad n\| | 0.885 |
| 最大主曲率 \|κ_max\| | **0.895** |
| \|Gauss曲率\| | 0.896 |
| 外形からの距離 | 0.444(**無意味**) |

> **Gauss曲率の0.896は数値的な人工物。**可展面では厳密にゼロのはずで、κ_max が大きい
> 箇所で二次当てはめの残差が増え、それを拾っているだけ。**この数字を根拠にしてはいけない。**

**採用すべきは κ_max。**可展面では κ_max が曲げの強さそのものを表す。

### 関連研究

- 可展面のセグメンテーションを**法線の不連続を疎に検出**する問題として定式化
  ([Mathematics 2026, 14(5), 757](https://doi.org/10.3390/math14050757))
- ruling を制御点から生成する B-spline 可展面
  ([arXiv:2004.09038](https://arxiv.org/pdf/2004.09038))
- 有理・NURBS 可展面の CAD における特徴づけ
  ([arXiv:2003.00792](https://arxiv.org/pdf/2003.00792))

---

## 13.2 展開図(flat pattern)— 同じ部品の別の見方

### 学術的事実

可展面は**等長に平面へ展開できる**。展開図は板金の実際の製造入力(ブランク形状)で、
3D形状と1対1に対応する。

展開すると:
- **曲げ線は直線になる**(円筒の ruling が平行線に写る)
- **外形は平面上の閉曲線になる**
- **面積と曲線長が保存される**

**これは「導出できるが別表現」という珍しい位置にある。**3D形状から計算できるので
KB 12 により入力にはならないが、**補助的な教師信号**としては別の勾配を与える。

### なぜ有望か

現在の生成器の失敗は「512個の変位を積分して形が壊れる」という**大域的な整合の問題**。
展開図は**大域的な拘束**(面積保存、長さ保存)を1つの量に凝縮する。

例えば「生成した曲げ線と外形から展開図を作り、その面積が教師と一致するか」は、
局所的な誤差では動かず、大域的な破綻でだけ動く。

### 関連研究

- エネルギー最小化による板金の展開
  ([ResearchGate](https://www.researchgate.net/publication/257337731_An_energy-based_surface_flattening_method_for_flat_pattern_development_of_sheet_metal_components))
- 展開手法の設計上の考慮
  ([J. Intell. Manuf. 2014](https://link.springer.com/article/10.1007/s10845-012-0679-9))
- **Flatten Anything** — 教師なしの神経曲面パラメータ化。Deform-Net / Wrap-Net /
  Cut-Net / Unwrap-Net の4つで双方向の写像を学ぶ
  ([arXiv:2405.14633](https://arxiv.org/pdf/2405.14633))
- **Flattening-Net** — 不規則な3D点群を規則的な2D幾何画像として表現
  ([IEEE TPAMI 2023](https://dlnext.acm.org/doi/10.1109/TPAMI.2023.3244828))

---

## 13.3 剛性 — なぜビードとリブが存在するか

### 学術的事実

薄板は面外曲げに弱い。ビード・リブ・フランジは**断面二次モーメント I を上げる**ために
入れる。矩形断面では I = bh³/12 なので、**高さ h の3乗**で効く。板厚を増やすより
形状を変えるほうが、質量あたりの剛性が桁違いに大きい。

**したがってビードの位置と向きは、剛性が必要な方向で決まる。**設計意図そのもの。

### 実測(本プロジェクト)

断面二次モーメントを入力に加えた場合の曖昧さの縮小:

| 与える情報 | 最も似た部品どうしの形状差 |
|---|---|
| 締結点のみ | 6.36mm |
| 締結点 + **断面二次モーメント** | **5.69mm(11%改善)** |
| 断面二次モーメントのみ | 7.50mm |

**11%は実在するが小さい。**そして代償として、テンソルは部品の向きと寸法を含むので、
以前除去した包絡リークと同じものを再導入することになる。

### 関連研究

- 荷重経路解析に基づく骨格構造の生体模倣設計。**断面の面積と慣性モーメントを最大化**する
  初期化で剛性 +62.3%、重量 −24.75%
  ([Appl. Sci. 2020, 10(22), 8251](https://doi.org/10.3390/app10228251))
- トポロジー最適化結果の中立面を**表面スケルトン**で再構成
  ([Proc. Design Society](https://www.cambridge.org/core/journals/proceedings-of-the-design-society/article/subdivision-surface-midsurface-reconstruction-of-topology-optimization-results-and-thinwalled-shapes-using-surface-skeletons/1E82A1D71A4DAF9B297269EE4BDBCF9B))
- 中立軸スケルトンによるトポロジー最適化構造の自動再構成
  ([Springer 2021](https://link.springer.com/chapter/10.1007/978-3-662-63143-0_3))

---

## 13.4 荷重経路 — 締結点から形が決まる唯一の物理的経路

### 学術的事実

部品は締結点間で荷重を伝える。**荷重経路**(load path)は、応力の主方向に沿った
流れとして定義され、トポロジー最適化ではこれが構造の骨格になる。

**これが本課題で最も筋の良い物理的事前分布**である理由:
- 入力(締結点)から**直接計算できる**
- 形状が未知でも、**荷重経路の候補**は境界条件だけから絞れる
- ビード・リブは荷重経路に沿って、または直交して置かれる

### 位置づけ

**ただし KB 12 が効く。**荷重経路は締結点から計算できるので、**モデルは既に知っている**
可能性が高い。効くとすれば「モデルが自力で計算するには深すぎる」場合だけ。

**測ってから判断すべき。**曲率場は「導出できる」側で30%悪化した前例がある。

### 関連研究

- 荷重経路解析による骨格設計(上記 Appl. Sci. 2020)
- 薄肉構造のトポロジー最適化と適応メッシュ
  ([arXiv:1908.10825](https://arxiv.org/pdf/1908.10825))
- 埋め込み固体 MMC による複雑薄肉構造の陽的設計
  ([arXiv:2306.10449](https://arxiv.org/pdf/2306.10449))

---

## 13.5 成形限界 — 何が作れないかの拘束

### 学術的事実

| 拘束 | 内容 |
|---|---|
| **最小曲げ半径** | 材料と板厚で決まる。R/t が下限を割ると外側が破断 |
| **スプリングバック** | 弾性回復。曲げ角は狙いより開く。R/t と降伏比で決まる |
| **成形限界線図(FLD)** | 主ひずみ空間での破断限界。伸びフランジ・張出しの可否 |
| **中立軸のシフト(K因子)** | 展開長 = 内R + K·t。K は 0.3〜0.5 |

### 本データでの実測

| | 値 |
|---|---|
| 曲げ半径 | 10.0〜22.5mm(中央値14.2) |
| 板厚 | サイドカーの `thickness_mm` にある |
| 曲げ角 | 0.5〜134.6°(中央値37.8) |

**R/t は計算可能。**「生成された曲げが製造可能か」の検証に直接使える。
**教師との距離が使えない本課題で、絶対的な合否条件になりうる数少ない量。**

---

## 13.6 抽出済みなのに使っていない情報

`runs/wtok_synth/wireframes/*.json` に既にあり、**変換器が捨てているもの**:

| フィールド | 内容 | 使えるか |
|---|---|---|
| `face_geo` | `planar` / `single_curved` / `double_curved` | **可展性の正解ラベル。有望** |
| `face_radius_mm` | 各面の半径 | **曲げ半径の正解。R/t 検証に直結** |
| `face_types` | `plane`/`cylinder`/`sphere`/`freeform`/`cone` | 面の分類 |
| `face_wall` | 壁面かどうか | フランジ・ビード壁の識別 |
| `dihedral_deg` | 辺での二面角 | **実測すると中央値0.0°で使えない**(§13.7) |
| `length_mm` | 辺の長さ | 自前で計算済み |
| `closed` | 辺が閉じているか | 連結処理で使える |
| `clusters` | `{'kind': 'bend', 'n_faces': 89}` | **曲げ領域のまとまり。未検証** |
| `n_planar_faces` / `n_wall_faces` | 面数の内訳 | 部品の複雑さの指標 |

**`face_geo` と `face_radius_mm` が最も有望。**可展性と曲げ半径という、
材料力学的に意味のある2量の正解値が、既に抽出されている。

---

## 13.7 使えないと分かったもの

### `dihedral_deg` は中央値 0.0°

| 辺の型 | 中央値 | 範囲 |
|---|---|---|
| `bend_line` | **0.00°** | 0.0〜3.3 |
| `surface_frame` | 0.00° | 0.0〜2.4 |
| `seam` | 0.46° | 0.0〜2.2 |

**曲げ線の二面角はゼロ。**当然で、`bend_line` は円筒面と平面が**接する**線
(接線連続)を印付けているため。二面角で曲げの強さは測れない。

### 外形からの距離は無意味(AUC 0.444)

偶然以下。曲げ線は外形との距離では特徴づけられない。

### Gauss曲率は数値的にゼロにならない

理論上ゼロだが、点群からの推定では κ_max に相関した残差が乗る。
**AUC 0.896 は信号ではなく人工物。**

---

## 13.8 提案する優先順位

| # | 施策 | 分類 | 期待 | コスト |
|---|---|---|---|---|
| 1 | **`face_geo` / `face_radius_mm` を抽出に通す** | 教師信号 | 可展性と曲げ半径の正解が得られる | 変換器の修正のみ |
| 2 | **R/t を合否条件にする** | 検証 | 絶対的な合格条件が1つ増える | 小 |
| 3 | **展開図の面積・長さ保存を補助損失に** | 教師信号 | 大域的な整合を1量に凝縮 | 中 |
| 4 | **板厚を条件に加える** | 入力 | 締結点から導出できない | 小 |
| 5 | 荷重経路を条件に加える | 入力? | **導出可能なので効かない恐れ**。オラクルで先に測る | 中 |
| 6 | 断面二次モーメントを条件に | 入力 | 11%。ただし包絡リークの再導入 | 中 |

**1と2から始めるべき。**抽出済みの情報を捨てているだけなので、費用が最も小さく、
材料力学的に意味のある量が2つ増える。

---

## 13.9 本プロジェクトの原則との整合

> **性質は表現に埋め込め、損失に書くな**(4勝4敗)

可展性は**表現に埋め込める**性質である。ruling を明示的に持つ表現なら、
κ₁·κ₂=0 が構造的に保証される。これは順序付きリングが閉ループを保証したのと同じ形。

**ただし実データには `double_curved` が存在する**ので、可展性を厳密に強制すると
ビードの四隅が表現できなくなる。**「可展を優先し、必要なら破れる」表現が要る。**

> **導出できる情報を条件に足しても効かない**(4回失敗)

本節の候補の大半は導出可能側にある。**入力ではなく教師信号・検証として使うこと。**
これが今回の整理の主要な結論。


---

<!-- 元文書: KB\18_catia_edges.md -->

# 18. CATIAのエッジ分割を教師にする — 設計提案

Date: 2026-08-31
発端: ユーザーの指摘「CATIA上では座面付近の円弧は2つの円弧で、基準面の曲げは1つの円弧で
表現されていた。CATIAのエッジの分け方は1つの合理性であり、汎用的に模倣させてはどうか」。

**結論: 指摘は測定で裏付けられた。現在の教師はCATIAの頂点構造を捨てて作り直しており、
外形の残課題(長辺上の角の揺れ=余剰回転+30°)の根はそこにある。**

---

## 18.0 現状の抽出は何をしているか

抽出は2段になっている:

1. `wireframe_app` → `runs/wtok_synth/wireframes/*.json`
   **B-repエッジをそのまま**保持(エッジごとに polyline、フル精度)。**CATIAの頂点構造はここまで生きている。**
2. `wtok/convert.py` → `runs/wtok_synth/parts/*.json`(現在の教師)
   エッジ列を**溶接して1本のチェーンに繋ぎ直し、0.35mm許容差で再帰的にLINE/ARC分割**。
   **分割点はフィット都合で置かれ、CATIAの頂点は消える。**

## 18.1 測定(60部品)

| 問い | 測定 | 結果 |
|---|---|---|
| CATIAの頂点と再当てはめの角は一致するか | 再当てはめ角 → 最近傍CATIA頂点距離 | **中央値1.89mm、62%が1mm超**。エッジ数は近い(16対17)が、**置き場所が違う** |
| 生のCATIA外形の接合はどれだけ滑らかか | エッジ端の接線角(生polyline、〜2mmスパン) | **G1(<3°) 10.7% / 3〜15° 82.1% / 真の角(≥15°) 7.2%** |
| 量子化後の教師の接合 | 同、`parts` の角 | ほぼ全てが角に見える(接線連続 0%) |

**読み**: CATIAの外形は3Dでは大部分が滑らかに繋がる(折り部を横切る所は円筒面に接して曲がる)。
真の角は1部品に1〜2個しかない。現在の教師は、その滑らかな連なりを0.35mmの弦で折り、
**「本当は存在しない角」を辺の途中に発明**している。モデルはそれを忠実に学び、
角の位置が部品ごとに恣意的なぶん**揺れる** — これが余剰回転 +30° の正体で、
学習を600エポックに伸ばすと位置は0.5mm改善するのに揺れが30°悪化した(KB 17.5)こととも整合する。
KB 16.2 の「接線連続の弧は15%しかない」も、再当てはめが接線を壊した結果だった。

座面の「2つの円弧」もCATIAの頂点(分割点)であり、現在の教師では消えたり動いたりしている。

## 18.2 提案

**教師の分割をCATIAのエッジ分割に置き換え、接合の種別を表現に載せる。**
形状ルールは書かない — CATIAが既に下した分割の決定を、そのまま学習対象にする。

| # | 変更 | 内容 |
|---|---|---|
| 1 | `convert.py` の再帰フィット廃止(外形) | **B-repエッジ1本 = プリミティブ1本**。頂点はCATIAの頂点。種別はそのエッジ単体への当てはめ(分割の曖昧さが無いので許容差論争が消える)。当てはめ残差はエッジ単位で記録し、残差が大きいエッジ(スプライン)は当面ARCで近似して残差を報告 |
| 2 | 接合チャンネルの追加 | スロットごとに1ch: **前の辺との接合が接線連続(G1)か、角(G0)か**。教師値は生polylineの接線角(<3°)から。分類ではなく連続性という幾何量 |
| 3 | 実現時のG1強制 | `realize_frame` で、G1フラグの立つ接合は**接線が一致するように構造で保証**(弧の膨らみ・角位置を接合条件から解く)。「揺れても偽の角にならない」を表現で保証 — 哲学1の適用 |
| 4 | 評価への追加 | 層2に「接合種別の一致率」(G1をG0にした/逆)を追加。**真の角(7%)を正しい場所に置けたか**が簡潔性の中身になる |

期待する効果:
- 長辺の途中の角はG1接合として生成され、実現時に滑らかに繋がる → **余剰回転の+30°が構造的に消える**
- 座面の2弧分割・折りの1弧といった**CATIAの合理性**がそのまま目標になる
- is_arc のラベル雑音問題(KB 16.2)は、分割が固定されることで大幅に軽くなる

## 18.3 検証(1・2は測定済み — 提案の成立条件は満たされている)

1. ✅ **エッジ単位の当てはめ残差**(60部品、1084エッジ): **全エッジが1本のLINEまたはARCに
   残差ほぼ0で当たる**(0.25t超え 0.0%。ARC 511 / LINE 573)。スプラインも再分割も不要 —
   **CATIAのエッジは最初からプリミティブ1本**であり、0.35mm許容差の再帰フィットは
   「既に切れているものを別の場所で切り直していた」ことになる
2. ✅ **容量**: エッジ数 中央値16・最大24 < EDGE_SLOTS 32
3. 新教師で `frame_rel` と同条件で再学習 → **KB 17の3層評価+接合一致率**で比較(拘束+ランクは共通)
4. 画像は3D斜め視点(`rational_3d.png` と同形式)

## 18.4 リスクと備え

- **座標量子化**: `parts` の bin 量子化(±0.3mm)は接線を壊す。接合チャンネルの教師値は
  量子化前の生polylineから計算して持ち込む(量子化後から再計算しない)
- CATIAの分割が部品作成手順に依存して揺れる可能性 → 同一形状の別バッチで分割の再現性を測る
- G1強制の解が過拘束になるケース(両隣ともG1の短い辺) → 最小二乗で解き、残差を層1に計上

## 18.5 実装記録(2026-08-31、承認済み)

実装: `convert.add_outline_primitives`(エッジ単位フィット+クラスタ溶接+接合角 `jt`)、
`codec.quantize_W` が `jt` を運搬、`frame.FRAME_CH=12`(ch11 = G1フラグ、閾値3°)、
`frame.enforce_g1`(実現時に弧の膨らみを接合条件から局所解、両端拘束は平均+残差)、
`rational.junction_match`(層2: 接合一致率)、`rational_eval.py`(標準評価ハーネス、
scratchpad消失の再構築)。

**測定方法の改良**: 接合角は提案の「生polyline 2mmスパン」ではなく
**フィット済みプリミティブの端点接線**から取る(残差~0なのでこれが生の幾何)。60部品:

| 測り方 | G1(<3°) | 曖昧(3-15°) | 角(≥15°) |
|---|---|---|---|
| 2mmスパン弦(18.1) | 10.7% | 82.1% | 7.2% |
| プリミティブ接線(採用) | **78%** | 18% | **4%** |

18.1の「82%が曖昧」は**スパンが弧の曲率を角と読んでいた**。厳密接線では分布が割れ、
3°閾値は測定に裏付けられた。

**新教師(2300部品再構築、`runs/wtok_synth_g1`)**: 変換失敗0、round-trip 2300/2300、
忠実度0.40mm(旧と同等)、外形エッジ 中央値16・最大22(<32)、フィット残差
部品内最大の中央値0.077mm・最悪0.324mm、frame_target再構成0.449mm(旧0.453)。
教師の座面比1.000、G1シェア0.73-0.78。

**新事実**:
- **CATIA教師は板厚未満の実在短辺を持つ**(辺の8.9%。全てLINE、中央0.70mm、座面から
  中央43mm — 座面由来ではない)。旧教師は正準化で消していた。層1の「スライバー=0」判定は
  新教師では**教師床との相対**で読むこと
- 抽出器は**長さ0.008mmのゴミ外形エッジ**を吐く(除外した)。グリッド丸め溶接は
  0.008mm差の端点を別セルに落として1部品目でループを切った → **クラスタ溶接**に置換
- ハーネス較正: 教師行は17.5と一致(1.009 / 100% / 188°)。生成側の絶対値は消えた
  scratchpad版と一致しない(medoid 0.81/307° 対 0.76/219°)— **比較は同一ハーネス内のみ有効**

**画像**: `runs/wtok_synth_g1/teacher_view.png` — 新教師12部品。灰=実現曲線(G1強制済み)、
赤=真の角(G0)、緑=G1接合、青×=旧refit教師の角。青×が辺の途中(CATIA頂点の無い場所)に
落ちる様子が「発明された角」の可視化。G1 16/16(全接合が接線)の部品も複数ある。

**学習**: Kaggle `autometalsheet-frame-g1` — g1a/g1b(300ep×2、実行間ばらつき用)+
g1long(900ep)。rel+spec、frame_relと同処方。**反証可能な予測**: KB 17.5の
「長く回すと余剰回転+30°」は発明された角の位置揺れが原因なので、新教師では
**長時間学習で余剰回転が悪化しないはず**。g1longがこれを直接試す。
(旧教師側の対照: 掃引v12の4アーム評価は KB 17.5 に記録 — どれも frame_rel から分離せず)

## 18.6 結果(2026-08-31、学習完了)

全て `rational_eval.py`(val 30、K=9、拘束+ランク)。**g1a と g1b はビット同一**だった —
ランチャーがシードを変えないため同一学習が2回走った(次の掃引は sweep JSON に seed を足すこと)。
実行間ばらつきは未推定(B5: 各構成1回の学習)。

| | 座面比 | ≥0.95 | 角ベース余剰(教師床) | 近傍誤差 |
|---|---|---|---|---|
| frame_rel(旧教師300ep) | 1.000 | 73% | 198°(188°) | 4.93mm |
| 旧教師 900ep(掃引long) | 0.977 | 67% | 258°(188°) | 4.49mm |
| g1a(新教師300ep) | 1.016 | 90% | 253°(207°) | 4.59mm |
| **g1long(新教師900ep)** | 1.000 | **97%** | **235°**(207°) | **4.08mm** |

**予測は当たった。**旧教師では 900ep が余剰回転最悪(+70°)、新教師では 900ep が**全指標で
300ep より良い**(余剰 −18°、座面合格 +7pt、近傍 −0.5mm)。「位置損失を下げるほど角が揺れる」
矛盾は教師の発明された角が原因で、除去すれば長時間学習は普通に効く。

**実現曲線の余剰回転**(絵が見るもの。Σ|回転|−360°、教師床は両者205°で同一):

| | 教師 | 生成(ランク) | 教師比 |
|---|---|---|---|
| frame_rel(旧) | 204° | 478° | **+274°** |
| g1long(新) | 205° | 309° | **+104°** |

**ギザギザの62%が消えた。**絵でも確認済み(`runs/kaggle_output/frame_g1long/rational_eval/
overlay_3d.png`): 長辺の揺れは見えなくなり、旧モデルで自己交差していた 0012 も外形に追従。
残る欠陥は形状レベルの取り違え(0044 の逸走、0078 の凹ローブ)で、角の揺れではない —
条件の情報不足の領分(KB 12)。

**接合一致率**: g1_as_g0 = 0.00(接線接合に偽の角を置かない)。ただし **g0_missed 12%** —
モデルは G1 フラグをほぼ常時 1 に丸め(生成シェア中央値 1.00 対 教師 0.91)、実在する角の
12% を接線として実現してしまう。フラグの較正(閾値 or 損失重み)が次の残課題。

**採用**: 新教師(`runs/wtok_synth_g1`)+ G1 表現を外形の標準とする。

## 18.7 G1較正とミクロ破綻の是正(2026-08-31)

ユーザー指摘「0012/0044にミクロで破綻したエッジが残る」への測定と対策。全て g1long、val 30、K=9。

### G1フラグ「較正」は棄却 — フラグは既に判定を下している

フラグ出力は実質2値(真G0での中央値0.003 / 真G1で1.002)。閾値0.5〜0.995で混同行列は不変
(g0_missed 38.5%固定)= **較正の余地なし、38.5%は判定誤り**。
モデル自身の描画角にも信号はある(G0 18.4° / G1 9.6°、AUC 0.72)が、**衝突帯の分解**が決定打:

| フラグ=1 かつ描画角 | n | P(真G0) |
|---|---|---|
| 0〜8° | 148 | 1.4% |
| 8〜15° | 106 | 3.8% |
| 15〜25° | 66 | **9.1%** |
| ≥25° | 50 | **16.0%** |
| フラグ=0(全角度) | 43 | 74.4% |

**フラグと幾何が矛盾するとき、84〜91%でフラグが正しい**(大きく描かれた折れの大半は、
モデルが下手に描いた真のG1で、強制が直している)。幾何で上書きすると平滑化勝ち102件を
捨てて角10件しか救えない。→ **フラグ強制は現行維持**。g0_missed 12%(部品中央値)は
学習側の残課題(次スイープで、シード修正と合わせて扱う)。

### ミクロ破綻の帰属と対策(すべて同一ハーネスでA/B)

| 症状 | 帰属(測定) | 対策 | 結果 |
|---|---|---|---|
| 針(0012等、150°超の折返し) | **モデルの生描画に存在**(座面投影・G1強制は無罪 — 生描画と同数、座面から33〜57mm)。9件全て突出0.1〜1.7mm(≦1.2t)の亀裂状 | **`rational.despike`**: 突出<2tの150°超針の角を削除(教師は30/30無変化 = 教師クリーン) | スパイク10%→**0%**、近傍4.08→**3.97mm**、座面97%維持 |
| G1強制が針を作る解 | `_bulge_from_tangent` がほぼ全周の弧を許容 | 矢高>弦/2の解を棄却(`enforce_g1` ガード、残差に計上) | 実装済み |
| スパイクの強ゲート(選択) | **A/Bで棄却**: 乱れた部品では唯一の良形状描画が針を持ち、ゲートが棄却(>8mm失敗4→5) | ランクの**最終タイブレーク**に格下げ | 形状無傷でスパイク低減 |
| 合意中央値の簡潔性基準 | **A/Bで棄却**: 4→5部品悪化、0125が9.0→15.5mm | 基準0のまま | — |
| 平面投影の自己交差判定 | **教師が17%引っかかる測定器欠陥**(折り部品は1投影で必ず重なり得る) | 診断表示のみ(層1・ランクから除外) | 生成23% 対 教師17% |
| 逸走(0044/0045/0125/0052) | 0052は正解モードが9回中2回出るが、**座面も合意も誤モードを支持**(誤形状は座面を楽に満たし、多数派が誤モード)。教師なし信号での選択は不能 | **モデル側課題として記録**(KB 12の情報不足領分)。候補: フロー自身の尤度による再ランク、K増 | 30部品中4部品 |

### 最終状態(g1long、despike込み、`rational_eval2/`)

| | 座面比 | ≥0.95 | 余剰回転 | スパイク | 近傍誤差 |
|---|---|---|---|---|---|
| 教師 | 1.000 | 100% | 207° | 0% | — |
| 中央選択 | 0.932 | 47% | 243° | 10% | 4.50mm |
| **拘束+ランク+despike** | **1.000** | **97%** | 231° | **0%** | **3.97mm** |

## 18.8 G1連続値チャンネルでの再学習 — 採用(2026-09-01)

8cfdb0f(G1チャンネルをビット→滑らかさ 1−jt/45° の連続値に変更)の初学習。
ローカル GTX 1660 SUPER、2200部品(=全2300−val100)、900ep×**2シード直列**
(10.6秒/ep、計約6h)。評価は `rational_eval.py`(val 30、K=9、拘束+ランク+despike)、
g1longと同一ハーネス。**合格条件は学習前に記載**(g0_missed 12%→半減以下、他指標非悪化)。

| ranked | g1long(基準) | g1sm seed1(ep660) | g1sm seed2(ep840) |
|---|---|---|---|
| **g0_missed** | 12% | **0.00** | **0.00** |
| g1_as_g0 | 0.00 | 0.00 | 0.00 |
| 座面≥0.95 | 97% | **100%** | **100%** |
| 近傍誤差 | 3.97mm | 2.51mm | **2.13mm** |
| 角ベース余剰(床207°) | 235° | 225° | 231° |
| スパイク | 0% | 0% | 0% |

**全条件を両シードでクリア。採用。**標準チェックポイントは `runs/frame_g1sm2/best.pt`
(ep840。near最良だがシード選択は事後的 — 運用上の指定であり優劣の主張ではない)。

**絵の所見**(`runs/frame_g1sm{1,2}/rational_eval/overlay_3d.png`): 長辺の揺れ・針は
見えない。角は正しい位置で鋭い。残る欠陥は既知のモード取り違えのみ(0044は別形状、
0078は凹ローブ。18.7の「選択不能」領分、変化なし)。

**実行間ばらつきの初測定**(18.6以来の宿題): ranked近傍 2.13 vs 2.51mm(±0.2mm)。
**medoid段の座面≥0.95は 73% vs 87% と大きく揺れる** — 構成比較をmedoid値で行っては
ならない(rankedは収束する)。B1/B5の習慣に定量的根拠が付いた。

**交絡の明記**: 本学習は「G1連続値」と「部品数1000→2200」を**同時に**変えた。
g0_missed 12%→0 はチャンネル変更の設計どおりの効果とみてよいが、**近傍3.97→2.1mmの
帰属は未分離**。D3の記録(過学習ゼロ=データ増は効かない)と矛盾する規模なので、
対照アーム(連続値+1000部品、約1.5h)を回せば確定する — **未実施**。

### 18.8 補遺: フロー尤度ランクは採用見送り(2026-09-01測定)

18.7の残課題「逸走4部品への尤度ランク」を確率流ODE逆積分+Hutchinson発散で測定
(g1long、30部品×9描画、24step×2プローブ):

- **corr(logp, 近傍誤差) 中央値 +0.00** — 尤度は形状の正しさの全体信号にならない
- 選択を logp に置換すると中央値 3.97→4.32mm と**悪化**
- 逸走4部品では 0052 が 11.7→4.3mm と改善する一方、0045/0125 は不変〜悪化。
  部品ごとの相関も +0.44〜−0.42 と不安定

→ **採用しない。**逸走はモード選択の問題ではなく条件情報の問題(KB 12)という
18.7の結論を補強。なお私の g1angle300(1000部品×300ep)は、rcセッションの
frame_g1sm2(2200部品×900ep×2シード、KB 18.8本文で採用)に包含されるため破棄。
